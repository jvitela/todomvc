<todos-header oninput={{createTodo}}></todos-header>

<todos-list 
  todos={{visibleTodos}} 
  onremove={{removeTodo}} 
  onchange={{editTitle}} 
  oncompleted={{toggleCompleted}}
  onediting={{toggleEditing}}
  ></todos-list>

{{#if todos.length}}
<todos-footer 
  filter={{filter}}
  todos={{todos}} 
  clear-completed={{clearCompleted}}></todos-footer>
{{/if}}